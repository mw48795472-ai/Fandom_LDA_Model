# archive/ — 최종 코퍼스 확정 이전 시점 백업

| 파일 | 내용 |
|---|---|
| `v6_r22_era_backup.zip` | v7 r22 시점(5,612건) 자료 52개: 당시 코퍼스 `fandoms_v3_100.json`, 점수, 진단, 파일럿 노트북, 토크나이저 초기 스크립트, 상세명세서. 보고서 본문은 참조하지 않는다 |

## 같은 이름, 다른 시점 — 주의

zip 안의 `data/v6_r22_snapshot/`에는 `data/v7_final/`과 **이름이 같은 파일**이 있다. 파일명의 `v6`은 파이프라인 버전이지 스냅샷 시점이 아니다.

| 이름 | zip 안 (r22 시점) | `data/v7_final/` (동결 v7-40) |
|---|---|---|
| `fandoms_v3_100.json` | 5,612건 (사이먼도미닉·창모·헤이즈 포함 구 로스터) | 10,020건 라이브 코퍼스 |
| `fandom_scores_v6.json` / `.csv` | activity 합 5,612 | activity 합 7,350 |
| `lda_v6_diagnostics.json` | K=8·M=6·실루엣 0.154 | `lda_v6_diagnostics_frozen_v7_40.json`으로 이름을 바꿔 보관, K=10·M=5·실루엣 0.267 |

동결 스냅샷(7,350건)의 값이 필요하면 항상 `data/v7_final/`의 파일을 쓴다. zip 안의 파일은 r22 시점 대비용이다.

## 이 zip에 없는 것

동결 7,350건 코퍼스 원본, 14개 언어 라우팅 토크나이저(`run_lda_v6_live_reference_v7.py`), 동결 모델의 φ·코사인 거리 행렬은 이 zip에도, git 이력 어디에도 없다(내용 기준으로 전수 검색함).

다만 동결 코퍼스는 `data/v7_final/frozen_snapshot_v7_40/`에 근사 복원본(7,326/7,350건)이 있고, 그 복원본에서 동결 이후 교체된 BE'O·pH-1·한로로 3개 팬덤의 문장은 **이 zip의 r22 시점 코퍼스에서 가져온 것**이다(28·34·33건). 이 zip이 보고서 본문에 기여하는 유일한 지점이다. 자세한 내용은 루트 README 9절.
